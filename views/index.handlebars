<!DOCTYPE html>
<html lang="en-us">
  <head>
    <title>Game of Life</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
  </head>

  <body class="game_of_life">
    <div id="main_gol_div">
      <div id="game_of_life_div" class="container-fluid"></div>
      <div id="controls_div">
      <h1>Conway's Game of Life</h1>

        <!--Primary Buttons-->
        <div class="buttons_div">
          <button id="new_game" class="btn btn-primary" onclick="game.setup();">New Game</button>
          <button id="run_game" class="btn btn-primary" onclick="game.run_game();">Run</button>
          <button id="pause" class="btn btn-primary" onClick="game.pause();">Pause</button>
          <br>
        </div>

        <!--Secondary Buttons-->
        <div class="container" id="secondary_buttons">
          <input title="Disabled until game start. Game will pause when color changed." id="color_picker" type="color" value="#C165EC">
          <button id="grid_collapse" class="btn btn-secondary" onclick="game.collapse_border()">Toggle Grid</button>
          <button id="more_info_button" class="btn btn-info">Info</button>
          <span class="backgroundtooltiptext">Change may take a few seconds.</span>
        </div>

        <!--Game of Life Wiki More Info modal button-->
        <div id="more_info" class="modal">
          <div id="more_info_modal">
            <span class="close">&times;</span>
            <p class="more_info_text">
              From Wikipedia: {{context.wiki_text}}
            </p>
            <p class="more_info_text">
              Patterns will continue developing until they stabalize into stable or repeating forms. Try changing the color and cycle speed!
            </p>

          </div>
        </div>

        <!--Speed slider bar-->
        <div class="slidecontainer">
            <input type="range" min="600" max="1000" value="800" class="slider" id="myRange">
            <label for="myRange">Cycle Speed</label>
        </div>
      </div>
    </div>
  </body>
</html>